# تجربة الموقع محليًا على جهازك (Windows) قبل الرفع

⚠️ **مهم:** إضافة Live Server في VS Code (اللي كنت تستخدمها سابقًا) **لا تكفي هنا** — هي تعرض ملفات HTML ثابتة فقط، ولا تشغّل كود PHP إطلاقًا. لازم بيئة PHP حقيقية.

## الطريقة الأسهل: تثبيت PHP مباشرة + سيرفر PHP المدمج

### 1. تثبيت PHP
1. حمّل PHP من الرابط الرسمي: **https://windows.php.net/download/**
   - اختر **"Thread Safe"** آخر إصدار متاح ضمن 8.1 أو أحدث (مثلاً 8.2 أو 8.3) → ZIP.
2. فك الضغط في مسار بسيط، مثلاً: `C:\php`
3. أضف `C:\php` إلى متغير البيئة PATH:
   - ابحث عن "Environment Variables" في قائمة ابدأ.
   - عدّل متغير `Path` وأضف `C:\php`.
4. تأكد من التثبيت — افتح PowerShell أو CMD جديد واكتب:
   ```
   php -v
   ```
   إذا ظهر رقم الإصدار، التثبيت نجح.

### 2. تفعيل الإضافات المطلوبة (SQLite وGD)
1. داخل مجلد `C:\php`، انسخ ملف `php.ini-development` وأعد تسميته إلى `php.ini`.
2. افتح `php.ini` بأي محرر نصوص (أو VS Code)، وابحث (Ctrl+F) عن هذه الأسطر واحذف الفاصلة المنقوطة `;` من بدايتها (يعني فعّلها):
   ```
   extension=sqlite3
   extension=pdo_sqlite
   extension=gd
   extension=mbstring
   ```
3. احفظ الملف.

### 3. تشغيل الموقع
1. افتح PowerShell أو CMD.
2. انتقل لمجلد المشروع (المكان اللي فككت فيه ملف الـ zip):
   ```
   cd "C:\path\to\portfolio-v2"
   ```
3. شغّل سيرفر PHP المدمج:
   ```
   php -S localhost:8000
   ```
4. افتح المتصفح على:
   ```
   http://localhost:8000/setup.php
   ```
   وسوّي حساب المدير الأول (نفس الخطوات المذكورة في `DEPLOY.md`).
5. بعدها الموقع نفسه على `http://localhost:8000/index.php`، ولوحة التحكم على `http://localhost:8000/admin/`.

للإيقاف: ارجع لنافذة PowerShell واضغط `Ctrl + C`.

## بديل أسهل للمبتدئين: Laragon
لو تفضّل أداة بواجهة رسومية بدل التعامل مع PATH وphp.ini يدويًا:
1. حمّل **Laragon** (مجاني): https://laragon.org/download/
2. ثبّته، شغّله، اضغط "Start All" (يشغّل PHP + Apache تلقائيًا وبإضافات مفعّلة افتراضيًا).
3. انسخ مجلد المشروع داخل `C:\laragon\www\portfolio`.
4. افتح المتصفح على `http://portfolio.test/setup.php` (Laragon يسوّي رابط تلقائي بهذا الشكل).

هذي الطريقة أقرب لبيئة Hostinger الحقيقية (Apache حقيقي، يعني ملف `.htaccess` يشتغل ويقدر تتأكد من رؤوس الأمان أيضًا محليًا) — بعكس سيرفر PHP المدمج اللي **يتجاهل `.htaccess` تمامًا** (هذا طبيعي وليس خطأ، فقط لا تتفاجأ إذا رؤوس الأمان ما ظهرت وأنت مستخدم `php -S`).

## ملاحظة عن قاعدة البيانات أثناء التجربة المحلية
كل تجربة محلية (مشاريع تضيفها، رسائل تجريبية، حساب المدير) تُخزّن في `data/portfolio.sqlite` على جهازك فقط — **لا تُرفع هذا الملف للاستضافة**. على السيرفر الحقيقي، شغّل `setup.php` من جديد لإنشاء حساب مدير خاص بالموقع المباشر (بيانات مختلفة عن تجربتك المحلية إذا حبيت).
