# دليل رفع الموقع على Hostinger (خطوة بخطوة)

## 1. الرفع
1. افتح **File Manager** في لوحة Hostinger لموقع `engyusef.alpha-code.net`.
2. احذف كل الملفات القديمة من `public_html` (أو انقلها لمجلد نسخة احتياطية).
3. ارفع **كل محتويات** هذا المجلد إلى `public_html` مباشرة (يعني `index.php` نفسه يكون داخل `public_html`، مو داخل مجلد فرعي).

## 2. الصلاحيات (Permissions)
بعد الرفع، تأكد إن هذين المجلدين قابلين للكتابة (777 أو 775 حسب إعداد Hostinger):
- `data/`
- `assets/img/uploads/`

عادةً Hostinger يديها صلاحية كتابة تلقائيًا لأنها مملوكة لنفس مستخدم PHP، لكن لو ظهر خطأ "unable to open database file"، هذا أول شيء تتحقق منه من File Manager (يمين-كليك → Permissions).

## 3. إنشاء حساب المدير (مرة واحدة فقط)
1. افتح `https://engyusef.alpha-code.net/setup.php` في المتصفح.
2. اختر اسم مستخدم وكلمة مرور قوية (10 أحرف فأكثر) — هذي بيانات **خاصة فيك أنت فقط**، ما نعرفها ولا نقدر نشوفها.
3. بعد ما يظهر "تم إنشاء الحساب بنجاح" → **احذف ملف `setup.php` من الاستضافة فورًا**. هذا مهم جدًا أمنيًا، لأن ترك الملف يسمح لأي حد يعرف الرابط بمحاولة إنشاء حساب مدير جديد (لن ينجح لأنه يتحقق من وجود حساب مسبقًا، لكن لا داعي لهذه المخاطرة).

## 4. تسجيل الدخول للوحة التحكم
اذهب إلى `https://engyusef.alpha-code.net/admin/` وسجّل دخولك بالحساب اللي أنشأته.

من لوحة التحكم تقدر:
- تضيف/تعدّل/تحذف المشاريع (مع صورة، رابط GitHub، رابط تحميل) — تظهر فورًا في الموقع.
- تشوف رسائل نموذج التواصل الواردة من الزوار.
- تعدّل نص "عني" لكل لغة من اللغات الخمس.
- تغيّر كلمة مرورك.

## 5. تحقق أمني سريع بعد الرفع
افتح هذه الروابط في المتصفح — **كلها المفروض تعطيك خطأ 403 Forbidden** (يعني الحماية شغالة):
- `https://engyusef.alpha-code.net/data/portfolio.sqlite`
- `https://engyusef.alpha-code.net/includes/db.php`

لو أي واحد منها فتح بشكل طبيعي (مو 403)، خبرني فورًا لأنه يعني ملف `.htaccess` ما انفعّل على الاستضافة.

## 6. ملاحظة عن PHP
الموقع مبني ليعمل على PHP 8.1 فأعلى مع امتداد SQLite3 وGD (للصور) — تأكد من لوحة Hostinger أن هذين الامتدادين مفعّلان (Advanced → PHP Configuration). حسب لقطة الشاشة اللي أرسلتها، كلاهما مفعّل فعليًا على استضافتك.

## استكشاف الأخطاء
| المشكلة | الحل المحتمل |
|---|---|
| صفحة بيضاء تمامًا | فعّل عرض الأخطاء مؤقتًا أو راجع "Error Log" من لوحة Hostinger |
| "unable to open database file" | راجع صلاحيات مجلد `data/` |
| الصور ما ترفع من لوحة التحكم | راجع صلاحيات `assets/img/uploads/` وتأكد أن امتداد GD مفعّل |
| `admin/` يعطي 404 | تأكد إن الرفع كان لمحتويات المجلد وليس المجلد نفسه كمجلد فرعي إضافي |
